apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metric-db-persistent-volume-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi
